

In this folder I add posts in org-mode format (org extension) that I'm have
written. Notice that posts here are not read by Jekyll directly. First we
need to export them (using the /publish/ functionality of org-mode) to the
_posts folder in HTML format.
